name: tests

on:
  pull_request:
    branches: [main, master]

jobs:
  video-ocr:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start server
        run: |
          docker compose up --build -d &
          sleep 30

      # - name: Run tests
      #   run: |
      #     pytest run tests
